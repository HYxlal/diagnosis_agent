# diagnosis_agent - Python dependencies
# Install: pip install -r requirements.txt

# Core
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pyyaml>=6.0

# Input parsing
openpyxl>=3.1
xlrd>=2.0
pandas>=2.0

# CLI
typer>=0.9
rich>=13.0
prompt_toolkit>=3.0

# Vector store (required — ChromaDB)
chromadb>=0.4.18

# LLM / Agent (required — LangChain)
langchain>=1.3.0
langchain-core>=1.0.0
langchain-openai>=0.1.0
langchain-chroma>=1.0.0
langchain-community>=0.1.0

# Embedding (DashScope)
requests>=2.28

# Neo4j 故障知识图谱
neo4j>=5.22.0,<6.0

# Token 计数（上下文预算管理）
tiktoken

# Redis 会话存储（热层/温层持久化）
redis>=5.0

# Testing
pytest>=7.0
pytest-cov>=4.0
